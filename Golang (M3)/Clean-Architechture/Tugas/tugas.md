1. Repository simpan data mahasiswa (nim, nama, ipk)
2. Service validasi dan cari data mahasiswa berdasarkan nim
3. Handler ambil data parameter dari url kemudian panggil service untuk dapat data mahasiswa
Jika data mahasiswa tidak ditemukan tampilkan : "Data mahasiswa tidak ditemukan"

validasi service
nim harus lebih dari 6 karakter
nim tidak boleh kosong